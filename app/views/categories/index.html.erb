<h4 class="bg-primary text-center text-white pb-2">Categories</h4>
<section>
  <div class="container mb-5">
    <%= render partial: "category", collection: @categories %>
  </div>
  <%= link_to "Add New Category", new_category_path, class:"btn btn-primary rounded-0 w-100 position-fixed bottom-0" %>
</section>